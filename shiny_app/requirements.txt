shiny>=1.0.0
# Cap below 2.16: newer supabase pulls storage3/pyiceberg, which needs MSVC to build on Windows (e.g. Python 3.14).
supabase>=2.0.0,<2.16.0
requests>=2.28.0
python-dotenv>=1.0.0
pandas>=2.0.0
markdown>=3.5.0
# Agent 1 RAG: embeddings (same family as dsai/07_rag lab_embed_rag.py; pulls torch on first install).
sentence-transformers>=2.2.0
numpy>=1.24.0
